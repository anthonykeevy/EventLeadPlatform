# UAT Generate Workflow
# BMAD v6 Workflow for Ralf Taskflow Module
name: uat-generate
description: "Generate a deterministic UAT checklist from Task Spec and Completion Note"
author: "Ralf Taskflow Module"
version: "1.0.0"

# Critical variables from config
config_source: "{project-root}/bmad/ralf-taskflow/config.yaml"
output_folder: "{config_source}:output_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
date: system-generated

# Workflow components
installed_path: "{project-root}/bmad/ralf-taskflow/workflows/uat-generate"
instructions: "{installed_path}/instructions.md"
validation: "{installed_path}/checklist.md"
template: false

# Variables and inputs
variables:
  task_spec_path: ""
  completion_note_path: ""
  story_id: ""
  task_id: ""

# Output files
outputs:
  uat_checklist: "{output_folder}/{story_id}/{task_id}.uat.md"

required_tools:
  - read_file
  - write_file

recommended_inputs:
  - task_spec: "Task Spec markdown file"
  - completion_note: "Task Completion Note from ralf-dev"

tags:
  - uat
  - testing
  - validation
  - bmad-v6

execution_hints:
  interactive: true
  autonomous: false

standalone: true
