Block 2 iteration 2 addendum (single lever: company-default parameters):

Keep first-shot schema-valid output and keep a simple non-overlapping vertical stack of fields within canvas bounds.

For this iteration, replace basic component parameter assumptions with company-default style parameters:
- Use theme object (not string): `primaryColor=#0055FF`, `backgroundColor=#9CA3AF`, `fontFamily=Inter`.
- Include `globalStyles` aligned to company defaults with at least:
  - `fontFamily=Inter`
  - `defaultLayout=vertical`
  - `defaultObjectLayout=vertical`
  - `inputHeight=40`
  - `objectRowGapPx=24`
- Size and position controls using those company-default style values rather than ad hoc/basic component sizing.
